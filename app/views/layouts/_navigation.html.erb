<nav class="w-full bg-gradient-to-r from-gray-900 via-black to-gray-800 border-b border-gray-700 shadow-lg">
  <div class="max-w-5xl mx-auto flex items-center justify-between py-4 px-6">
    <!-- Logo/Brand -->
    <div class="flex items-center space-x-3">
      <%= link_to "Tiny Storage", root_path, class:"text-cyan-400 font-extrabold text-2xl tracking-widest select-none" %>

    </div>
    <!-- Navigation Buttons -->
    <div class="flex items-center space-x-4">
      <%= link_to "Upload File", "/upload",
                  class: "bg-cyan-400 hover:bg-cyan-500 text-black font-bold py-2 px-5 rounded-full transition shadow-md" %>
      <% if user_signed_in? %>
        <%= link_to "My Files", "/list",
        class: "bg-gray-800 border border-cyan-400 text-cyan-400 hover:bg-cyan-900 hover:text-cyan-300 font-bold py-2 px-5 rounded-full transition shadow-md" %>
        <span class="text-gray-300 ml-4 mr-2">Hello, <%= current_user.email %></span>
        <%= link_to "Sign Out", destroy_user_session_path, data: {turbo_method: :delete},
                    class: "bg-gray-800 border border-cyan-400 text-cyan-400 hover:bg-cyan-900 hover:text-cyan-300 font-bold py-2 px-5 rounded-full transition shadow-md" %>
      <% else %>
        <%= link_to "Sign In", new_user_session_path,
                    class: "bg-gray-800 border border-cyan-400 text-cyan-400 hover:bg-cyan-900 hover:text-cyan-300 font-bold py-2 px-5 rounded-full transition shadow-md" %>
      <% end %>
    </div>
  </div>
</nav>
